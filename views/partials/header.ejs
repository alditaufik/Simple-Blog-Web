<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="\style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    
</head>
<header class="border-bottom d-flex flex-wrap justify-content-between align-items-center ">
    <div class="container">
        <div class=" d-flex flex-warp align-item-center justify-content-center justify-content-lg-start">
            <div class="col-md-4 d-flex align-items-center">
                <ul class ="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="/home" class="nav-link px-2 link-secondary"> Home</a></li>
                    <li><a href="/about me" class="nav-link px-2 link-secondary"> about me</a></li>
                    <li><a href="/contact" class="nav-link px-2 link-secondary"> contact</a></li>
                </ul>
            </div>
            <div class="ms-auto d-flex align-items-center ustify-content-center justify-content-lg-end">
                <span >hello, 
                    <% if (user) { %>
                    <%= user.firstName +" "+ user.lastName %> 
                    <% } else { %>
                        Guest 
                        <% } %> 
                </span>
            </div> 
        </div>
    </div>
</header>

<div class="container">